drop table reg;

create table reg(
    fname varchar(255),
    lname varchar(255),
    rname varchar(255)
);

create table reg(
    fname varchar(255),
    lname varchar(255),
    rname varchar(255)

);

-- alter table reg add column verify_token varchar(255) default 'null';
-- alter table reg add column verify_token varchar(255) default 'null';
